<nav class="navbar navbar-expand-lg navbar-light bg-primary">
    <a class="navbar-brand" href="#">Wild Circus</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a class="nav-item nav-link" href="{{ path('admin_performance_index') }}">Performances</a>
            <a class="nav-item nav-link" href="{{ path('admin_ticket_index') }}">Events</a>
            <a class="nav-item nav-link" href="{{ path('admin_ticket_index') }}">Bookings</a>
            <a class="nav-item nav-link" href="{{ path('home') }}">Back to site</a>
            <a class="nav-item nav-link" href="{{ path('app_logout') }}">Log out</a>
        </div>
    </div>
</nav>
